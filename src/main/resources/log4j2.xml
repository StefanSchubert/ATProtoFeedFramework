<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="JSON_PATTERN">
            {"timestamp":"%d{ISO8601}","level":"%level","thread":"%t","logger":"%logger","message":"%enc{%m}{JSON}","exception":"%enc{%throwable}{JSON}"}
        </Property>
    </Properties>

    <Appenders>
        <Console name="ConsoleAppender" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <!-- JSON format for structured logging (production) -->
        <Console name="JsonConsoleAppender" target="SYSTEM_OUT">
            <JsonTemplateLayout eventTemplateUri="classpath:EcsLayout.json"/>
        </Console>

        <!-- Async appender for better performance -->
        <Async name="AsyncConsole">
            <AppenderRef ref="ConsoleAppender"/>
        </Async>
    </Appenders>

    <Loggers>
        <!-- Framework logger -->
        <Logger name="de.bluewhale.atprotofeed" level="info" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <!-- Spring Boot -->
        <Logger name="org.springframework" level="warn" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <!-- Hibernate -->
        <Logger name="org.hibernate" level="warn" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <!-- Flyway -->
        <Logger name="org.flywaydb" level="info" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <!-- Root logger -->
        <Root level="info">
            <AppenderRef ref="AsyncConsole"/>
        </Root>
    </Loggers>
</Configuration>
